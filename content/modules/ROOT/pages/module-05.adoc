In this challenge we’ll forward incoming tcp requests to rhelvm on port
`*9999*` to `*12345*`.

In the `*>_ controlnode*` terminal, add the lines
`*- forward_port: '9999/tcp;12345;'*` and `*state: enabled*`.

The following command will add those lines for you.

.Command
[source,bash,subs="+macros,+attributes",role=execute]
----
tee -a /root/hosts << EOF
      - forward_port: '9999/tcp;12345;'
        state: enabled
EOF
----

Now run the playbook in the `*>_ controlnode*`
terminal.

.Command
[source,bash,subs="+macros,+attributes",role=execute]
----
ansible-playbook -i hosts -b firewall.yml
----

.Output
[source,text,subs="+macros,+attributes"]
----
<<< OUTPUT ABRIDGED >>>

TASK [redhat.rhel_system_roles.firewall : Configure firewall] ************************************************************
ok: [rhelvm.{{ guid }}.internal] => (item={'service': 'http', 'state': 'enabled'})
ok: [rhelvm.{{ guid }}.internal] => (item={'port': ['9999/tcp'], 'state': 'enabled'})
changed: [rhelvm.{{ guid }}.internal] => (item={'forward_port': '9999/tcp;12345;', 'state': 'enabled'})

<<< OUTPUT ABRIDGED >>>

PLAY RECAP *************************************************
rhelvm.{{ guid }}.internal      : ok=9    changed=1    unreachable=0    failed=0    skipped=12   rescued=0    ignored=0
----

In the `*>_ rhelvm*` terminal, run `*nc*`, listening
on port 12345.

.Command
[source,bash,subs="+macros,+attributes",role=execute]
----
nc -l 12345
----

In the `*>_ controlnode*` terminal, run the `*nc*` command:

.Command
[source,bash,subs="+macros,+attributes",role=execute]
----
nc rhelvm 9999
----

Type words to test connectivity as you did before and press enter.

.Command
[source,bash,subs="+macros,+attributes",role=execute]
----
the quick brown fox jumps over the lazy dogs
----

In the `*>_ controlnode*` terminal, type `*ctrl-c*`
to exit `*nc*`.
